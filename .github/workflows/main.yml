name: CMake

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release

jobs:
  build-host:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            arch-name: linuxx86-64
          - os: ubuntu-24.04-arm
            arch-name: linuxarm64
          - os: windows-latest
            arch-name: windowsx86-64
            extra-flags: -DCMAKE_CXX_COMPILER=clang-cl -DCMAKE_C_COMPILER=clang-cl
          - os: macos-latest
            arch-name: osxuniversal
            extra-flags: -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64"

    runs-on: ${{ matrix.os }}
    name: "mrcal-jni - Build - ${{ matrix.arch-name }}"

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: 'true'
        fetch-depth: 0

    - name: Fetch tags
      run: git fetch --tags --force
    - run: git describe --tags

    - uses: actions/setup-java@v5
      with:
        java-version: 25
        distribution: temurin

    - name: Install deps (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt update
        sudo apt-get install -y cmake ninja-build gcc g++ liblist-moreutils-perl

    - name: Install deps (macOS)
      if: runner.os == 'macOS'
      run: sudo cpan -i List::MoreUtils

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/cmake_build -G Ninja -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DOPENCV_ARCH=${{ matrix.arch-name }} ${{ matrix.extra-flags }}

    - name: Build shared mrcal_jni
      # Build your program with the given configuration
      shell: bash
      run: cmake --build "${{ github.workspace }}/cmake_build" --target mrcal_jni

    - name: Build mrcal_jni_test
      if: runner.os != 'Windows'
      shell: bash
      run: cmake --build "${{ github.workspace }}/cmake_build" --target mrcal_jni_test

    - name: Run mrcal_jni_test
      if: runner.os != 'Windows'
      shell: bash
      # On arm64, run under QEMU to catch illegal instructions
      run: |
        if [[ "${{ matrix.arch-name }}" == "linuxarm64" ]]; then
          sudo apt-get update
          sudo apt-get install -y qemu-user gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
          chmod +x "${{ github.workspace }}/cmake_build/bin/mrcal_jni_test"
          qemu-aarch64 \
            -cpu cortex-a55 \
            -L /usr/aarch64-linux-gnu \
            -E LD_LIBRARY_PATH="${{ github.workspace }}/cmake_build/lib" \
            "${{ github.workspace }}/cmake_build/bin/mrcal_jni_test"
        else
          "${{ github.workspace }}/cmake_build/bin/mrcal_jni_test"
        fi

    - name: Gradle build
      # Build your program with the given configuration
      run: ./gradlew build

    - run: ./gradlew publish
      name: Publish
      env:
        ARTIFACTORY_API_KEY: ${{ secrets.ARTIFACTORY_API_KEY }}
      if: github.event_name == 'push' || startsWith(github.ref, 'refs/tags/v')

    - uses: actions/upload-artifact@v7
      with:
        name: libmrcal-jar-${{ matrix.arch-name }}
        path: ${{ github.workspace }}/build/libs/*.jar
